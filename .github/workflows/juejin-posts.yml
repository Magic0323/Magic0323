name: Update Blog Posts

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - shell: python3
        run: |
          print("python works")
          import urllib.request
          print("urllib works")
          import json
          print("json works")
          print("all good")